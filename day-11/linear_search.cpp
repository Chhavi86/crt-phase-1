#include <iostream>
using namespace std;

class solution{
    public:
           int linear_search(int nums[],int n, int target )
           {
            for (int i=1 ){

                if(nums(i)== target)
                return 0;
            }
            return -1;
           };
}
    